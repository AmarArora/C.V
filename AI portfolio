import { motion } from "framer-motion";
import QuoteBanner from "@/components/QuoteBanner";

const stats = [
  { num: "4+", label: "AI projects in pipeline" },
  { num: "∞", label: "Repeatable automation potential" },
  { num: "0→1", label: "Prototype to product mindset" },
  { num: "Real", label: "Business-focused outcomes" },
];

const aiCards = [
  { num: "01 / Input", title: "Data Goes In", desc: "AI systems learn from examples — text, numbers, images, logs. The more relevant data, the sharper the model becomes at recognising patterns." },
  { num: "02 / Learn", title: "The Model Trains", desc: "Algorithms adjust internal parameters millions of times until the model can reliably map inputs to correct outputs — like learning to spot a spam email." },
  { num: "03 / Decide", title: "Predictions & Actions", desc: "Once trained, AI makes real-time predictions: classify this email, summarize this document, suggest this stock, reply to this customer message." },
  { num: "04 / Automate", title: "Workflows Run Themselves", desc: "Connect AI decisions to actions via APIs and automation tools. A task that took 3 hours runs in 30 seconds — every single day, without human input." },
];

const flowSteps = [
  { icon: "📥", label: "Trigger" },
  { icon: "🧠", label: "AI Model" },
  { icon: "⚡", label: "Decision" },
  { icon: "🔗", label: "API Action" },
  { icon: "✅", label: "Outcome" },
];

const Index = () => {
  return (
    <div className="pt-[60px] min-h-screen relative z-[1]">
      {/* HERO */}
      <motion.section
        className="min-h-[calc(100vh-60px)] flex flex-col justify-center max-w-[960px] mx-auto px-7 py-20"
        initial={{ opacity: 0, y: 22 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 0.9 }}
      >
        <div className="font-mono text-[11px] tracking-[0.2em] uppercase text-primary mb-7 flex items-center gap-2.5">
          <span className="block w-8 h-px bg-primary" />
          Building AI-Powered Automation
        </div>
        <h1 className="font-display text-[clamp(42px,7vw,88px)] font-extrabold leading-[1.02] mb-7 tracking-tight">
          Turning Repetitive Work{" "}
          <span className="bg-gradient-to-r from-primary to-secondary bg-clip-text text-transparent">
            Into Intelligent Systems
          </span>
        </h1>
        <p className="text-[17px] text-content-muted max-w-[520px] leading-[1.75] mb-11">
          I build AI tools that automate workflows, cut manual effort, and create
          measurable value for businesses — from day-one prototypes to production pipelines.
        </p>
        <div className="flex gap-3.5 flex-wrap">
          <a
            href="/projects"
            className="inline-flex items-center gap-2 bg-primary text-primary-foreground font-display font-bold text-[13px] tracking-[0.04em] px-6 py-3 rounded transition-all hover:-translate-y-0.5 hover:shadow-[0_8px_28px_hsl(var(--phase-1)/0.25)]"
          >
            View Projects →
          </a>
          <a
            href="/contact"
            className="inline-flex items-center gap-2 font-mono text-xs tracking-[0.06em] px-6 py-3 border border-border rounded text-foreground transition-colors hover:border-secondary hover:text-secondary"
          >
            Get in Touch
          </a>
        </div>

        {/* STATS */}
        <motion.div
          className="flex flex-wrap border border-border rounded-lg overflow-hidden mt-[70px] max-sm:flex-col"
          initial={{ opacity: 0, y: 22 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.9, delay: 0.15 }}
        >
          {stats.map((s, i) => (
            <div
              key={i}
              className="flex-1 min-w-[160px] px-8 py-7 border-r border-border last:border-r-0 max-sm:border-r-0 max-sm:border-b max-sm:last:border-b-0"
            >
              <div className="font-display text-[32px] font-extrabold bg-gradient-to-r from-primary to-secondary bg-clip-text text-transparent mb-1">
                {s.num}
              </div>
              <div className="text-xs text-muted-foreground">{s.label}</div>
            </div>
          ))}
        </motion.div>
      </motion.section>

      {/* AI SECTION */}
      <section className="max-w-[960px] mx-auto px-7 pb-20 pt-24">
        <motion.div initial={{ opacity: 0, y: 22 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ duration: 0.8 }}>
          <span className="inline-block font-mono text-[10px] tracking-[0.18em] uppercase text-primary border border-primary px-2.5 py-0.5 rounded-sm mb-5">
            What is AI?
          </span>
          <h2 className="font-display text-[clamp(26px,4vw,42px)] font-extrabold mb-5">
            AI, Demystified in Plain English
          </h2>
          <p className="text-[15px] text-content-muted max-w-[600px] leading-[1.75]">
            Artificial Intelligence is software that learns from data and makes decisions —
            without being explicitly programmed for every scenario. Here's the core idea:
          </p>
        </motion.div>

        <div className="grid grid-cols-1 md:grid-cols-2 gap-3.5 mt-10">
          {aiCards.map((c) => (
            <motion.div
              key={c.num}
              initial={{ opacity: 0, y: 16 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="bg-card border border-border rounded-lg p-7 transition-all hover:border-secondary/35 hover:-translate-y-0.5"
            >
              <div className="font-mono text-[11px] text-secondary mb-3.5 tracking-[0.1em]">{c.num}</div>
              <h3 className="font-display text-base font-bold mb-2">{c.title}</h3>
              <p className="text-[13px] text-muted-foreground leading-[1.7]">{c.desc}</p>
            </motion.div>
          ))}
        </div>

        {/* Flow visualization */}
        <motion.div
          initial={{ opacity: 0, y: 16 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          className="mt-14 bg-card border border-border rounded-lg p-9 overflow-x-auto"
        >
          <h3 className="font-display text-[15px] font-bold mb-7 text-secondary">
            How an AI Automation Pipeline Works
          </h3>
          <div className="flex items-center gap-0 min-w-[500px]">
            {flowSteps.map((step, i) => (
              <div key={i} className="contents">
                <div className="flex-1 bg-surface-2 border border-border rounded-md px-4 py-3.5 text-center">
                  <div className="text-xl mb-1.5">{step.icon}</div>
                  <div className="font-mono text-[10px] text-muted-foreground tracking-[0.08em] uppercase">{step.label}</div>
                </div>
                {i < flowSteps.length - 1 && (
                  <span className="text-lg text-border px-2.5 shrink-0">→</span>
                )}
              </div>
            ))}
          </div>
        </motion.div>
      </section>

      <QuoteBanner quote="The real question is not whether machines think, but whether men do." author="B.F. Skinner" />
    </div>
  );
};

export default Index;
